#include <iostream>
#include <set>
#include <vector>

class AOStarSearch{
public:
    AOStarSearch(/* args */);
    ~AOStarSearch();

    bool operator() (double left, double right);
private:

};

AOStarSearch::AOStarSearch(/* args */)
{
}

AOStarSearch::~AOStarSearch()
{
}
